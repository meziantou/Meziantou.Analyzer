# MA0006 - Use String.Equals instead of equality operator
<!-- sources -->
Sources: [UseStringEqualsAnalyzer.cs](https://github.com/meziantou/Meziantou.Analyzer/blob/main/src/Meziantou.Analyzer/Rules/UseStringEqualsAnalyzer.cs), [UseStringEqualsFixer.cs](https://github.com/meziantou/Meziantou.Analyzer/blob/main/src/Meziantou.Analyzer.CodeFixers/Rules/UseStringEqualsFixer.cs)
<!-- sources -->

You should use `string.Equals` instead of `==` or `!=`, to make string comparison rules explicit.

````csharp
"" == "";

// Should be
string.Equals("", "", StringComparison.Ordinal);
````

````csharp
"" != "";

// Should be
!string.Equals("", "", StringComparison.Ordinal);
````

The rule is not reported where a constant expression is required, such as the initializer of a constant, an attribute argument, a default parameter value, a case label or a pattern, as `string.Equals` is not a constant expression:

````csharp
const string Value = "a";
const bool IsB = Value == "b"; // ok
````

## Additional resources

- [Best practices for comparing strings in .NET](https://learn.microsoft.com/en-us/dotnet/standard/base-types/best-practices-strings?WT.mc_id=DT-MVP-5003978#specifying-string-comparisons-explicitly)
- [String comparisons are harder than it seems](https://www.meziantou.net/string-comparisons-are-harder-than-it-seems.htm)
