# MA0128 - Use 'is' operator instead of SequenceEqual
<!-- sources -->
Sources: [UseIsPatternInsteadOfSequenceEqualAnalyzer.cs](https://github.com/meziantou/Meziantou.Analyzer/blob/main/src/Meziantou.Analyzer/Rules/UseIsPatternInsteadOfSequenceEqualAnalyzer.cs), [UseIsPatternInsteadOfSequenceEqualFixer.cs](https://github.com/meziantou/Meziantou.Analyzer/blob/main/src/Meziantou.Analyzer.CodeFixers/Rules/UseIsPatternInsteadOfSequenceEqualFixer.cs)
<!-- sources -->

In C# 11, the `MemoryExtensions.SequenceEqual` and `MemoryExtensions.Equals` can be simplified when the value is a constant string.

````c#
ReadOnlySpan<char> span = "foo";

_ = span.Equals("bar", StringComparison.Ordinal); // report diagnostic
_ = span is "bar";

_ = span.SequenceEqual("bar"); // report diagnostic
_ = span is "bar";
````

The rule is not reported when a `string` converted to a span is compared to an empty string. A `null` string is converted to an empty span, so `MemoryExtensions.SequenceEqual<char>(value, "")` is `true` when `value` is `null`, whereas `value is ""` is `false`. The comparisons to a non-empty string are still reported, as both forms are `false` for a `null` string.
